generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://Postgres:61841369@147.93.28.169:5432/biblia-estudio?sslmode=disable"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
